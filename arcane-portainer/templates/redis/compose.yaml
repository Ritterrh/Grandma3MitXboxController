services:
  redis:
    image: "redis:latest"
    container_name: ${CONTAINER_NAME:-redis}
    restart: "unless-stopped"
    ports:
      - "6379/tcp"
    volumes:
      - "data:/data"

volumes:
  data:
