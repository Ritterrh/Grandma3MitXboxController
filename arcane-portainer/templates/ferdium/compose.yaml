services:
  ferdium:
    image: "linuxserver/ferdium:latest"
    container_name: ${CONTAINER_NAME:-ferdium}
    restart: "unless-stopped"
    ports:
      - "3000:3000/tcp"
      - "3001:3001/tcp"
    environment:
      - "PUID=${PUID:-1000}"
      - "PGID=${PGID:-1000}"
      - "TZ=${TZ:-Etc/UTC}"
    volumes:
      - "/srv/lsio/ferdium/config:/config"
