services:
  commandbox:
    image: "ortussolutions/commandbox:latest"
    container_name: ${CONTAINER_NAME:-commandbox}
    restart: "unless-stopped"
    ports:
      - "8080/tcp"
      - "8443/tcp"
    environment:
      - "CFENGINE=${CFENGINE:-lucee@4.5}"
